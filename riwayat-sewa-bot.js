(async()=>{
 const list=document.querySelector('#botOrderList'),msg=document.querySelector('#botMessage');
 const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
 const money=n=>new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(Number(n||0));
 const date=v=>v?new Date(v).toLocaleString('id-ID',{dateStyle:'medium',timeStyle:'short'}):'-';
 const labels={creating:'Membuat bot',waiting_pairing:'Menunggu pairing',connecting:'Menghubungkan',connected:'Aktif',reconnecting:'Menyambungkan ulang',stopped:'Dihentikan',suspended:'Ditangguhkan',expired:'Kedaluwarsa',failed:'Gagal',error:'Bermasalah',logged_out:'Logout'};
 async function headers(){const s=await KivoAuth.session();if(!s)location.href='login.html';return{Authorization:`Bearer ${s.access_token}`,'Content-Type':'application/json'}}
 async function api(options={}){const r=await fetch('/api/bot-orders',{...options,headers:{...(await headers()),...(options.headers||{})}});const b=await r.json().catch(()=>({}));if(!r.ok)throw new Error(b.error||'Gagal memuat data.');return b}
 function render(orders){if(!orders.length){list.innerHTML='<div class="empty">Belum ada pesanan sewa bot.<br><br><a href="sewa-bot.html">Buka katalog Sewa Bot</a></div>';return}list.innerHTML=orders.map(o=>`<article class="bot-card"><div class="bot-card-head"><div><small>${esc(o.invoice)}</small><h3>${esc(o.product_name)}</h3><small>${esc(o.variant_name||'Paket utama')}</small></div><span class="status ${esc(o.status)}">${esc(labels[o.status]||o.status)}</span></div><div class="bot-grid"><div><span>Nomor WhatsApp</span><strong>${esc(o.phone)}</strong></div><div><span>Harga</span><strong>${money(o.amount)}</strong></div><div><span>Nama bot / Prefix</span><strong>${esc(o.bot_name||'KEIINEXUS')} / ${esc(o.prefix||'.')}</strong></div><div><span>Masa aktif sampai</span><strong>${date(o.expires_at)}</strong></div></div>${o.status==='waiting_pairing'?`<div class="pairing"><span>PAIRING CODE</span><strong>${esc(o.pairing_code||'Sedang dibuat...')}</strong><small>WhatsApp → Perangkat Tertaut → Tautkan dengan nomor telepon</small></div>`:''}${o.error_message?`<div class="bot-message">${esc(o.error_message)}</div>`:''}<div class="actions">${o.pairing_code?`<button class="primary" data-copy="${esc(o.pairing_code)}">Salin Pairing Code</button>`:''}${!['expired','deleted','failed'].includes(o.status)?`<a class="plugin-link" href="plugin-store.html?invoice=${encodeURIComponent(o.invoice)}">Kelola Plugin</a><button data-act="restart" data-invoice="${esc(o.invoice)}">Restart Bot</button><button data-act="pairing" data-invoice="${esc(o.invoice)}">Buat Pairing Baru</button>`:''}</div></article>`).join('');bind()}
 function bind(){list.querySelectorAll('[data-copy]').forEach(b=>b.onclick=async()=>{await navigator.clipboard.writeText(b.dataset.copy);b.textContent='Tersalin'});list.querySelectorAll('[data-act]').forEach(b=>b.onclick=async()=>{b.disabled=true;try{await api({method:'POST',body:JSON.stringify({invoice:b.dataset.invoice,action:b.dataset.act})});await load()}catch(e){alert(e.message)}finally{b.disabled=false}})}
 async function load(){msg.textContent='Memuat status bot...';try{const b=await api();render(b.orders||[]);msg.textContent=''}catch(e){msg.textContent=e.message}}
 document.querySelector('#refreshBots').onclick=load;await load();setInterval(load,8000);
})();
