(()=>{const $=s=>document.querySelector(s),rp=n=>new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(Number(n||0)),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));async function load(){const box=$('#dhList');box.innerHTML='<p class="dh-empty">Memuat transaksi...</p>';try{const r=await fetch('/api/deposit-history',{headers:await KivoAuth.authHeaders(),cache:'no-store'}),d=await r.json();if(!r.ok)throw new Error(d.error||'Gagal memuat riwayat.');$('#dhBalance').textContent=rp(d.balance);const label={pending:'Menunggu pembayaran',completed:'Berhasil',expired:'Kedaluwarsa',cancelled:'Dibatalkan',failed:'Gagal'};box.innerHTML=d.deposits?.length?d.deposits.map(x=>`<article class="dh-card"><div class="dh-card-top"><span class="dh-status ${esc(x.status)}">${esc(label[x.status]||x.status)}</span><time>${new Date(x.created_at).toLocaleString('id-ID',{dateStyle:'medium',timeStyle:'short'})}</time></div><strong>${rp(x.amount)}</strong><p>${x.status==='completed'?'Saldo sudah masuk ke akun.':x.status==='pending'?'Selesaikan pembayaran sesuai invoice.':'Transaksi tidak menambah saldo.'}</p><div><span>${esc(x.invoice)}</span><b>Total bayar ${rp(x.payment_amount)}</b></div></article>`).join(''):'<p class="dh-empty">Belum ada riwayat deposit.</p>'}catch(e){box.innerHTML=`<p class="dh-empty">${esc(e.message)}</p>`}}$('#dhRefresh').onclick=load;load()})();
