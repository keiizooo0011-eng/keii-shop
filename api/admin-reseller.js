import { optionalAuthUser } from './_auth-user.js';import { adminClient } from './_lib.js';
async function assertAdmin(db,user){const {data}=await db.from('admin_users').select('user_id').eq('user_id',user.id).maybeSingle();if(!data)throw Object.assign(new Error('Akses admin diperlukan.'),{status:403});}
export default async function handler(req,res){res.setHeader('Cache-Control','no-store');const user=await optionalAuthUser(req);if(!user)return res.status(401).json({error:'Unauthorized'});try{const db=adminClient();await assertAdmin(db,user);if(req.method==='GET'){const [{data:settings},{data:products},{data:members},{data:orders}]=await Promise.all([db.from('reseller_settings').select('*').eq('id','main').maybeSingle(),db.from('reseller_catalog_products').select('*').order('sort_order'),db.from('reseller_memberships').select('*').order('joined_at',{ascending:false}),db.from('reseller_orders').select('*').order('created_at',{ascending:false}).limit(100)]);return res.json({settings,products:products||[],members:members||[],orders:orders||[]})}if(req.method==='POST'){const a=String(req.body?.action||'');if(a==='settings'){const {data,error}=await db.from('reseller_settings').upsert({id:'main',is_open:!!req.body.is_open,join_price:Number(req.body.join_price||0),title:String(req.body.title||''),subtitle:String(req.body.subtitle||''),updated_at:new Date().toISOString()}).select().single();if(error)throw error;return res.json({settings:data})}if(a==='product'){const id=req.body.id||undefined,payload={name:String(req.body.name||'').trim(),category:String(req.body.category||'apk-premium'),description:String(req.body.description||'')||null,image_url:String(req.body.image_url||'')||null,price:Number(req.body.price||0),stock:Number(req.body.stock||0),delivery_mode:req.body.delivery_mode==='automatic'?'automatic':'manual',requires_email:!!req.body.requires_email,variants:Array.isArray(req.body.variants)?req.body.variants:[],promo_text:String(req.body.promo_text||'')||null,is_active:req.body.is_active!==false,sort_order:Number(req.body.sort_order||0),updated_at:new Date().toISOString()};if(!payload.name)throw new Error('Nama produk wajib diisi.');let q=id?db.from('reseller_catalog_products').update(payload).eq('id',id):db.from('reseller_catalog_products').insert(payload);const {data,error}=await q.select().single();if(error)throw error;if(payload.delivery_mode==='automatic'&&Array.isArray(req.body.stock_lines)&&req.body.stock_lines.length){const rows=req.body.stock_lines.map(line=>{const [variant_name,email,password,...note]=String(line).split('|');return{product_id:data.id,variant_name:variant_name||'Paket utama',content:[email,password,...note].filter(Boolean).join('|')}});const ins=await db.from('reseller_stock_items').insert(rows);if(ins.error)throw ins.error;await db.from('reseller_catalog_products').update({stock:await countStock(db,data.id)}).eq('id',data.id)}return res.json({product:data})}if(a==='member'){const status=['active','suspended','expired'].includes(req.body.status)?req.body.status:'suspended';const {data,error}=await db.from('reseller_memberships').update({status,updated_at:new Date().toISOString()}).eq('id',req.body.id).select().single();if(error)throw error;return res.json({member:data})}if(a==='order'){const status=['processing','completed','cancelled'].includes(req.body.status)?req.body.status:'processing';const {data:o,error:oe}=await db.from('reseller_orders').select('*').eq('id',req.body.id).single();if(oe)throw oe;if(status==='cancelled'&&!o.refunded){const rf=await db.rpc('wallet_refund',{p_user_id:o.user_id,p_amount:Number(o.amount),p_reference:`REFUND:RESELLER:${o.invoice}`,p_description:`Refund reseller ${o.invoice}`,p_metadata:{order_id:o.id}});if(rf.error)throw rf.error;await db.from('reseller_catalog_products').update({stock:Math.max(Number((await db.from('reseller_catalog_products').select('stock').eq('id',o.product_id).single()).data?.stock||0)+1,0)}).eq('id',o.product_id)}const patch={status,admin_note:String(req.body.admin_note||'')||null,refunded:status==='cancelled'?true:o.refunded,completed_at:status==='completed'?new Date().toISOString():o.completed_at,updated_at:new Date().toISOString()};const {data,error}=await db.from('reseller_orders').update(patch).eq('id',o.id).select().single();if(error)throw error;return res.json({order:data})}return res.status(400).json({error:'Aksi tidak dikenal.'})}if(req.method==='DELETE'){const id=String(req.query?.id||'');const {error}=await db.from('reseller_catalog_products').delete().eq('id',id);if(error)throw error;return res.json({ok:true})}return res.status(405).json({error:'Method tidak diizinkan.'})}catch(e){return res.status(e.status||500).json({error:e.message||'Gagal memproses admin reseller.'})}}
async function countStock(db,id){const {count}=await db.from('reseller_stock_items').select('id',{count:'exact',head:true}).eq('product_id',id).eq('status','available');return Number(count||0)}