import { optionalAuthUser } from './_auth-user.js';import { adminClient, invoiceId } from './_lib.js';export default async function handler(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='POST')return res.status(405).json({error:'Method tidak diizinkan.'});const user=await optionalAuthUser(req);if(!user)return res.status(401).json({error:'Silakan masuk terlebih dahulu.'});let debited=false,amount=0,invoice='';try{const db=adminClient(),productId=String(req.body?.product_id||''),variantIndex=Number(req.body?.variant_index||0),customerName=String(req.body?.customer_name||'').trim(),customerContact=String(req.body?.customer_contact||'').trim(),customerEmail=String(req.body?.customer_email||'').trim().toLowerCase();if(!productId||!customerName||!customerContact)return res.status(400).json({error:'Data order belum lengkap.'});const {data:m}=await db.from('reseller_memberships').select('*').eq('user_id',user.id).eq('status','active').maybeSingle();if(!m)return res.status(403).json({error:'Akses reseller tidak aktif.'});const {data:p,error:pe}=await db.from('reseller_catalog_products').select('*').eq('id',productId).eq('is_active',true).maybeSingle();if(pe)throw pe;if(!p)return res.status(404).json({error:'Produk reseller tidak tersedia.'});const variants=Array.isArray(p.variants)&&p.variants.length?p.variants:[{name:'Paket utama',price:p.price}],variant=variants[variantIndex]||variants[0];amount=Number(variant.price??p.price);if(p.requires_email&&!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(customerEmail))return res.status(400).json({error:'Email aktif wajib diisi.'});if(Number(p.stock||0)<=0)return res.status(409).json({error:'Stok atau slot produk habis.'});invoice=invoiceId();const debit=await db.rpc('wallet_debit',{p_user_id:user.id,p_amount:amount,p_reference:`RESELLER-ORDER:${invoice}`,p_description:`Order reseller ${p.name}`,p_metadata:{reseller_product_id:p.id,invoice}});if(debit.error){if(String(debit.error.message||'').includes('INSUFFICIENT_BALANCE'))return res.status(402).json({error:'Saldo KivoPay tidak cukup.',need_deposit:true});throw debit.error}debited=true;let deliveryContent=null,status='pending';if(p.delivery_mode==='automatic'){const {data:stock,error:se}=await db.from('reseller_stock_items').select('*').eq('product_id',p.id).eq('status','available').eq('variant_name',String(variant.name||'Paket utama')).limit(1).maybeSingle();if(se)throw se;if(!stock)throw new Error('Stok otomatis untuk varian ini habis.');deliveryContent=stock.content;status='completed';await db.from('reseller_stock_items').update({status:'sold',sold_at:new Date().toISOString()}).eq('id',stock.id)}const {data:o,error:oe}=await db.from('reseller_orders').insert({invoice,user_id:user.id,reseller_code:m.reseller_code,product_id:p.id,product_name:p.name,variant_name:String(variant.name||'Paket utama'),amount,customer_name:customerName,customer_contact:customerContact,customer_email:customerEmail||null,delivery_mode:p.delivery_mode,requires_email:p.requires_email,payment_method:'balance',status,delivery_content:deliveryContent,paid_at:new Date().toISOString(),completed_at:status==='completed'?new Date().toISOString():null}).select().single();if(oe)throw oe;await db.from('reseller_catalog_products').update({stock:Math.max(Number(p.stock)-1,0),updated_at:new Date().toISOString()}).eq('id',p.id);return res.json({order:o,payment_method:'balance',balance_after:debit.data?.balance})}catch(e){if(debited&&user)try{await adminClient().rpc('wallet_refund',{p_user_id:user.id,p_amount:amount,p_reference:`REFUND:RESELLER:${invoice}`,p_description:`Refund ${invoice}`,p_metadata:{reason:e.message}})}catch{}return res.status(500).json({error:e.message||'Gagal membuat order reseller.'})}}